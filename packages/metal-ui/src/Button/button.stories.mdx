import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Button from "./index";
import Icon from "../../util/Icon";
import ArrowUpOutline from "@heroicons/react/outline/ArrowUpIcon";
import Box from "../Box";

<Meta title="Button" component={Button} />

# API

<Props of={Button} />

# Variants

<Preview>
  <Story name="Variants">
    <Box flex justifyContent="evenly">
      <Button label="Primary" />
      <Button label="Secondary" variant="secondary" />
      <Button label="Danger" variant="danger" />
      <Button label="Minimal" variant="minimal" />
    </Box>
  </Story>
</Preview>

# Sizes

<Preview>
  <Story name="Size">
    <div>
      <Box flex justifyContent="evenly">
        <Button label="Primary" size="xsmall" />
        <Button label="Primary" size="small" />
        <Button label="Default" />
        <Button label="Primary" size="large" />
      </Box>
      <div class="mt-2">
        <Button label="Block small" size="xsmall" block />
      </div>
      <div class="mt-2">
        <Button label="Block small" size="small" block />
      </div>
      <div className="mt-2">
        <Button label="Block normal" block />
      </div>
      <div className="mt-2">
        <Button label="Block large" size="large" block />
      </div>
    </div>
  </Story>
</Preview>

# Disabled

Disabled buttons do not inherit `variant` styling.

<Preview>
  <Story name="Disabled">
    <Box flex justifyContent="around">
      <Button label="Disabled" disabled />
      <Button
        label="Disabled"
        disabled
        iconBefore={<ArrowUpOutline width={20} height={20} />}
      />
      <Button
        label="Disabled"
        disabled
        size="large"
        iconBefore={<ArrowUpOutline width={20} height={20} />}
        iconAfter={<ArrowUpOutline width={20} height={20} />}
      />
    </Box>
  </Story>
</Preview>

# Loading

<Preview>
  <Story name="Loading">
    <Box flex justifyContent="around">
      <Button label="Loading" size="xsmall" isLoading={true} />
      <Button label="Loading" isLoading={true} />
      <Button label="Loading" variant="secondary" isLoading={true} />
      <Button label="Loading" variant="danger" isLoading={true} />
    </Box>
    <Box flex justifyContent="around">
      <Button label="Loading" block isLoading={true} />
    </Box>
  </Story>
</Preview>

# With Icon

The component does not handle icon sizing. You should manage that from your project. `20px` is usually the recommended size.

<Preview>
  <Story name="With Icon">
    <Box flex justifyContent="around">
      <Button
        iconBefore={<ArrowUpOutline width={12} height={12} />}
        label="Icon before"
        size="xsmall"
      />
      <Button
        iconBefore={<ArrowUpOutline width={20} height={20} />}
        label="Icon before"
      />
      <Button
        iconAfter={<ArrowUpOutline width={20} height={20} />}
        variant="secondary"
        label="Icon after"
      />
      <Button
        iconBefore={<ArrowUpOutline width={20} height={20} />}
        iconAfter={<ArrowUpOutline width={20} height={20} />}
        variant="secondary"
        label="Icon before and after"
      />
    </Box>
  </Story>
</Preview>

# With `next/link`

For use in conjunction with NextJS' `next/link`, wrap the button with `<Link>` and set `passHref` to true.

```javascript
<Link href="https://www.lattiude.sh" passHref>
  <Button label="Go to website" />
</Link>
```
