import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import Dropdown from "./index";
import Button from "../Button";

<Meta title="Dropdown" component={Dropdown} />

# Default

<Preview>
  <Story name="Default">
    <div className="flex h-full items-center justify-center w-full my-10">
      <Dropdown content={<span>Dropdown content</span>}>
        <Button label="Open dropdown" />
      </Dropdown>
    </div>
  </Story>
</Preview>

# Position

<Preview>
  <Story name="Position">
    <div className="flex h-full items-center justify-center w-full my-10">
      <Dropdown position="RightBottom" content={<span>Dropdown content</span>}>
        <Button label="Right Bottom" type="secondary" />
      </Dropdown>
      <Dropdown className="ml-2" position="RightTop" content={<span>Dropdown content</span>}>
        <Button label="Right Top" type="secondary" />
      </Dropdown>
      <Dropdown className="ml-2" position="LeftTop" content={<span>Dropdown content</span>}>
        <Button label="Left Top" type="secondary" />
      </Dropdown>
      <Dropdown className="ml-2" position="LeftBottom" content={<span>Dropdown content</span>}>
        <Button label="Left Bottom" type="secondary" />
      </Dropdown>
    </div>
  </Story>
</Preview>