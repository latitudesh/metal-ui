import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import {RadioGroup, RadioGroupItem, RadioGroupTitle, RadioGroupDescription} from "./index";


export const regions = [
  {
    name: 'London'
  },
  {
    name: 'Santiago',
    description: 'Chile',
  },
  {
    name: 'Dallas',
    description: 'United States',
  },
  {
    name: 'Chicago',
    description: 'United States',
  }
];


<Meta title="RadioGroup" component={RadioGroup} />

# API

### RadioGroup
<Props of={RadioGroup} />

### RadioGroupItem
<Props of={RadioGroupItem} />

### RadioGroupTitle
<Props of={RadioGroupTitle} />

# Default

<Preview>
  <Story name="Default">
     <RadioGroup  
    defaultValue="option-1"
  >
   <RadioGroupItem id="option-1" value="option-1">
     <RadioGroupTitle htmlFor="option-1">
       Option 1
      </RadioGroupTitle> 
      <RadioGroupDescription> Optional Description </RadioGroupDescription>
   </RadioGroupItem>  
  <RadioGroupItem id="option-1" value="option-2">
      <RadioGroupTitle htmlFor="option-2">
        Option 2
        </RadioGroupTitle>  
    </RadioGroupItem>  
  </RadioGroup>
  </Story>
</Preview>

# Card

<Preview>
  <Story name="Card">
    <form action="">
      <RadioGroup
      name="r-card"   
      defaultValue="London"
      variant="card"
      onValueChange={(value) => console.log("change: " + value)}
      >
      {regions.map((item, index) => (
          <RadioGroupItem value={item.name} key={index} id={item.name}>
            <RadioGroupTitle htmlFor={item.name}>{item.name}</RadioGroupTitle>
            { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
          </RadioGroupItem>
      ))} 
      </RadioGroup>
    </form>
  </Story>
</Preview>

# Grid

<Preview>
    <Story name="Grid">
      <form action="">
      <RadioGroup
      name="r-grid"
      defaultValue="London"
      variant="card"
      display="grid"
      onValueChange={(value) => console.log("change: " + value)}
      >
        {regions.map((item, index) => (
          <RadioGroupItem value={item.name} key={index}>
            <RadioGroupTitle>{item.name}</RadioGroupTitle>
            { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
          </RadioGroupItem>
      ))}
      </RadioGroup>
      </form>
    </Story>
  
</Preview>

# Collapsed 

<Preview>
    <Story name="Collapsed">
      <form action="">
      <RadioGroup
      name="r-grid"
      collapsed
      variant="card" 
      onValueChange={(value) => console.log("change: " + value)}
      >
        {regions.map((item, index) => (
          <RadioGroupItem value={item.name} key={index}>
            <RadioGroupTitle>{item.name}</RadioGroupTitle>
            { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
          </RadioGroupItem>
      ))}
      </RadioGroup>
      <RadioGroup
      className="mt-6"
      name="r-grid"
      display="grid" 
      variant="card" 
      collapsed
      onValueChange={(value) => console.log("change: " + value)}
      >
        {regions.map((item, index) => (
            <RadioGroupItem value={item.name} key={index}>
              <RadioGroupTitle>{item.name}</RadioGroupTitle>
              { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
            </RadioGroupItem>
      ))}
      </RadioGroup>
      </form>
    </Story>
  
</Preview>


# Disabled

<Preview>
  <Story name="Disabled">
    <RadioGroup 
    name="r-disabled"
    defaultValue="London"
    disabled={true} 
    onValueChange={(value) => console.log("change: " + value)}
    >
      {regions.map((item, index) => (
        <RadioGroupItem value={item.name} key={index}>
          <RadioGroupTitle>{item.name}</RadioGroupTitle> 
          { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
        </RadioGroupItem>   
    ))}
    </RadioGroup>
    <h3 className="my-6 font-bold">Disabled Card</h3>
    <RadioGroup 
    variant="card"
    name="r-disabled"
    defaultValue="London"
    disabled={true} 
    onValueChange={(value) => console.log("change: " + value)}
    >
       <RadioGroupItem value='option-1'>
          <RadioGroupTitle> Option 1 </RadioGroupTitle> 
          <RadioGroupDescription> Enabled </RadioGroupDescription>
        </RadioGroupItem>   
       <RadioGroupItem value='option-2'>
          <RadioGroupTitle> Option 2 </RadioGroupTitle> 
          <RadioGroupDescription> Disabled </RadioGroupDescription>
        </RadioGroupItem>   
       <RadioGroupItem value='option-3'>
          <RadioGroupTitle> Option 3 </RadioGroupTitle> 
          <RadioGroupDescription> Enabled </RadioGroupDescription>
        </RadioGroupItem>   
    </RadioGroup>
    <h3 className="my-6 font-bold">Disabled Item</h3>
    <RadioGroup 
    name="r-disabled"
    defaultValue="London"
    onValueChange={(value) => console.log("change: " + value)}
    >
       <RadioGroupItem value='option-1'>
          <RadioGroupTitle> Option 1 </RadioGroupTitle> 
          <RadioGroupDescription> Enabled </RadioGroupDescription>
        </RadioGroupItem>   
       <RadioGroupItem value='option-2' disabled={true}>
          <RadioGroupTitle> Option 2 </RadioGroupTitle> 
          <RadioGroupDescription> Disabled </RadioGroupDescription>
        </RadioGroupItem>   
       <RadioGroupItem value='option-3'>
          <RadioGroupTitle> Option 3 </RadioGroupTitle> 
          <RadioGroupDescription> Enabled </RadioGroupDescription>
        </RadioGroupItem>   
    </RadioGroup>
  </Story>
</Preview>

# Hidden Indicator

<Preview>
  <Story name="Hidden Indicator">
    <form action="">
      <RadioGroup
      name="r-hidden-indicator"
      defaultValue="London"
      variant="card"
      showIndicator={false}
      onValueChange={(value) => console.log("change: " + value)}
      >
      {regions.map((item, index) => (
          <RadioGroupItem value={item.name} key={index}>
            <RadioGroupTitle>{item.name}</RadioGroupTitle>
            { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
          </RadioGroupItem>
      ))}
      </RadioGroup>
    </form>
  </Story>
</Preview>

# Multiple labels

<Preview>
  <Story name="Multiple labels">
    <form action="">
      <RadioGroup
      name="r-hidden-indicator"
      defaultValue="London"
      variant="card"
      showIndicator={false}
      onValueChange={(value) => console.log("change: " + value)}
      >
      {regions.map((item, index) => (
          <RadioGroupItem value={item.name} key={index} id={item.name}>
            <div className="flex justify-between">
              <div>
                <RadioGroupTitle htmlFor={item.name}>{item.name}</RadioGroupTitle>
                { item.description && <RadioGroupDescription>{item.description}</RadioGroupDescription>}
              </div>
              <div>
                <RadioGroupTitle asLabel={false}>$297</RadioGroupTitle>
                 <RadioGroupDescription>/mo</RadioGroupDescription>
              </div>
            </div>
          </RadioGroupItem>
      ))}
      </RadioGroup>
    </form>
  </Story>
</Preview>