import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import {
  Toolbar,
  ToolbarButton,
  ToolbarLink,
  ToolbarSeparator,
  ToolbarToggleGroup,
  ToolbarToggle,
} from "./index";
import {
  Dropdown,
  DropdownTrigger,
  DropdownContent,
  DropdownLabel,
  DropdownItem,
  DropdownSeparator,
} from "../DropdownMenu";

<Meta title="Toolbar" component={Toolbar} />

# Available components

Metal exports the following components for the Toolbar:

- Toolbar
- ToolbarButton
- ToolbarLink
- ToolbarSeparator
- ToolbarToggleGroup
- ToolbarToggle

# API

This component uses the Radix UI `Toolbar` primitive. Refer to [this page](https://radix-ui.com/primitives/docs/components/toolbar) for the API reference, but note components in Metal UI do not use dot-notation.

# Default

<Preview>
  <Story name="Default">
    <Toolbar>
      <ToolbarButton>Button 1</ToolbarButton>
      <ToolbarButton>Button 2</ToolbarButton>
      <ToolbarSeparator />
      <ToolbarLink href="https://www.maxihost.com" target="_blank">
        Link
      </ToolbarLink>
      <ToolbarSeparator />
      <ToolbarToggleGroup type="single" defaultValue="center">
        <ToolbarToggle value="left">Left</ToolbarToggle>
        <ToolbarToggle value="center">Center</ToolbarToggle>
        <ToolbarToggle value="right">Right</ToolbarToggle>
      </ToolbarToggleGroup>
    </Toolbar>
  </Story>
</Preview>

# With DropdownMenu

You can render the DropdownMenu as part of the toolbar

<Preview>
  <Story name="With DropdownMenu">
    <Toolbar>
      <Dropdown>
        <DropdownTrigger as={ToolbarButton}>Alphabetical</DropdownTrigger>
        <DropdownContent>
          <DropdownItem>Recently updated</DropdownItem>
          <DropdownItem>Least recently updated</DropdownItem>
        </DropdownContent>
      </Dropdown>
    </Toolbar>
  </Story>
</Preview>
