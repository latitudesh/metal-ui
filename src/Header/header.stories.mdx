import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import Header from "./index";
import Button from "../Button";
import { SwitchVerticalOutline } from "heroicons-react";
import {
  Toolbar,
  ToolbarButton,
  ToolbarLink,
  ToolbarSeparator,
  ToolbarToggleGroup,
  ToolbarToggle,
} from "../Toolbar";
import {
  Dropdown,
  DropdownTrigger,
  DropdownContent,
  DropdownLabel,
  DropdownItem,
  DropdownSeparator,
} from "../DropdownMenu";

<Meta title="Header" component={Header} />

# Default

<Preview>
  <Story name="Default">
    <Header title="Title" />
  </Story>
</Preview>

# With action

<Preview>
  <Story name="With action">
    <Header title="Title" actions={<Button label="Action" />} />
  </Story>
</Preview>

# With nav

<Preview>
  <Story name="With nav">
    <Header
      title="Title"
      actions={<Button label="Action" />}
      nav={
        <Toolbar>
          <ToolbarButton>Button</ToolbarButton>
          <ToolbarLink href="https://www.maxihost.com" target="_blank">
            Link
          </ToolbarLink>
          <ToolbarSeparator />
          <ToolbarToggleGroup type="single" defaultValue="center">
            <ToolbarToggle value="left">Left</ToolbarToggle>
            <ToolbarToggle value="center">Center</ToolbarToggle>
            <ToolbarToggle value="right">Right</ToolbarToggle>
          </ToolbarToggleGroup>
        </Toolbar>
      }
    />
  </Story>
</Preview>

# With filters

<Preview>
  <Story name="With filters">
    <Header
      title="Title"
      actions={<Button label="Action" />}
      nav={
        <Toolbar>
          <ToolbarButton>Page 1</ToolbarButton>
          <ToolbarButton>Page 2</ToolbarButton>
          <ToolbarButton>Page 3</ToolbarButton>
        </Toolbar>
      }
      filters={
        <Toolbar>
          <Dropdown>
            <DropdownTrigger as={ToolbarButton}>
              Alphabetical
            </DropdownTrigger>
            <DropdownContent align="end">
              <DropdownItem onSelect={() => console.log("cut")}>
                Cut
              </DropdownItem>
              <DropdownItem onSelect={() => console.log("copy")}>
                Copy
              </DropdownItem>
              <DropdownItem onSelect={() => console.log("paste")}>
                Paste
              </DropdownItem>
            </DropdownContent>
          </Dropdown>
        </Toolbar>
      }
    />
  </Story>
</Preview>
