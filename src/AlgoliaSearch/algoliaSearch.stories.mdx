import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import AlgoliaSearch from "./index";

<Meta title="AlgoliaSearch" component={AlgoliaSearch} />

# Default
Try searching for a movie title like `The Shawshank Redemption`

<Preview>
  <Story name="Default">
    <div style={{ height: '300px' }}>
      <AlgoliaSearch
        ALGOLIA_APP_ID="XWSX3ZKORI"
        ALGOLIA_API_SEARCH_KEY="bc85ff85b128ba7dfc859b986cc9df21"
        indices={[
          {
            indexName: "movies",
            displayName: "Movies",
            renderCardInfo: (data) => <p>{data.title}</p>,
            formatHitURL: (data) => `/${data.title}`,
          }
        ]}
        searchOperators={["<", "=", ">", ">=", "<="]}
        specialChar=":"
        scrollWindowHeight={400}
      />
    </div>
  </Story>
</Preview>

# Multiple indexes

<Preview>
  <Story name="Multiple indexes">
    <div style={{ height: '300px' }}>
      <AlgoliaSearch
        ALGOLIA_APP_ID="62STYGLZOF"
        ALGOLIA_API_SEARCH_KEY="37b26ab5c38bc1512f846d6a355b8116"
        indices={[
          {
            indexName: "devices",
            displayName: "Servers",
            renderCardInfo: (data) => <p>{data.description}</p>,
          },
          {
          indexName: "zendesk_maxihost_articles",
          displayName: "Documentation",
          renderCardInfo: (data) => <p>{data.title}</p>,
          formatHitURL: (data) => `/${data.title}`,
        },
        ]}
        searchOperators={["<", "=", ">", ">=", "<="]}
        specialChar=":"
      />
    </div>
  </Story>
</Preview>

