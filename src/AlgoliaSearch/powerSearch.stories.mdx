import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import AlgoliaSearch from "./index";

<Meta title="AlgoliaSearch" component={AlgoliaSearch} />

# Default

<Story name="Default">
  <AlgoliaSearch
    ALGOLIA_APP_ID="latency"
    ALGOLIA_API_SEARCH_KEY="6be0576ff61c053d5f9a3225e2a90f76"
    indices={[{
      indexName: 'instant_search',
      displayName: 'Movies',
      renderCardInfo: (hit) => <p>{hit.name}</p>,
      formatHitURL: (hit) => `${hit.url}`,
    }]}
    searchOperators={['<', '=', '>', '>=', '<=']}
    specialChar=":"
    scrollWindowHeight={400}
  />
</Story>