import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import Button from "./index";
import Icon from "../../util/Icon";

<Meta title="Button" component={Button} />

# Default

<Preview>
  <Story name="Default">
      <div>
    <div>
      <Button label="Action" />
      <Button label="Danger" type="secondary" />
      <Button label="Danger" type="dark" />
      <Button label="Danger" type="danger" />
      <Button label="Disabled" disabled />
    </div>
    <div className="mt-4">
      <Button component={<a href="javascript:void()">With component</a>} />
      <Button iconBefore={<Icon />} type="secondary" label="Icon before" />
      <Button iconAfter={<Icon />} type="secondary" label="Icon after" />
    </div>
    </div>
  </Story>
</Preview>
